<div class="col-md-4">
          <div class="p-4 mb-3 bg-dark rounded">
            <h4 class="font-italic">About Me</h4>
            <p class="mb-0">Passionate about all things technology and sports. I've worked in QA for just over 4
            years now and am looking to take that knowledge of helping to create quality software
            and transition into a web developer role.</p>
          </div>
          <div class = 'p-4'>
            <h4>Topics</h4>
            <% @side_bar_topics.each do |topic| %>
              <p><%= link_to topic.title, topic_path(topic) %></p>
            <% end %>
          </div>
      
          <%= render 'shared/socials' %>
</div>